\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{array}
\usepackage{multirow}
\usepackage{listings}
\usepackage{float}
\usepackage{xcolor}
\usepackage{bbm}


% Configure listings for Python
\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    showstringspaces=false,
    tabsize=4
}

\geometry{margin=1in}

\title{Formal and Empirical Verification of Compositional Robustness and Scalability of Mixture-of-Experts Architecture}
\author{Connor Pham}
\date{November 2025}

\begin{document}

\maketitle

\section{Abstract}

\section{Introduction}

Deep neural networks (DNNs) achieve state-of-the-art accuracy in vision and language tasks but remain vulnerable to small adversarial perturbations, an unacceptable risk for safety-critical systems such as autonomous driving or medical imaging. In parallel, the Mixture-of-Experts (MoE) paradigm provides scalable and modular computation by routing inputs to specialized subnetworks. However, while large homogeneous MoE models (e.g., GShard, Switch Transformer, Mixtral) demonstrate remarkable scalability, their robustness and verifiability, especially in heterogeneous settings, remain poorly understood.

Formal verification tools like $\alpha$--$\beta$-CROWN now enable provable robustness guarantees for neural networks, yet their integration with modular, multi-expert architectures is underexplored. Bridging empirical adversarial robustness and formal certification is essential for building verifiable AI systems that are both practical and theoretically sound.

This paper introduces MetaMoE, a heterogeneous, verifiable MoE architecture composed of Adversarially trained experts and a formally verified gating network. We study how robustness propagates across modules and how empirical robustness (under PGD attacks) correlates with certified robustness accuracy (CRA). The work contributes:

\begin{enumerate}
    \item A compositional robustness framework linking adversarial training and formal verification.
    \item Verification-aware CNN architecture designed for $\alpha - \beta$-CROWN.
    \item Empirical--formal correlation analysis showing $AA \approx CRA - \delta$.
    \item Scalability results showing 16.7\% faster incremental training and linear inference scaling
\end{enumerate}

Together, these results provide a unified empirical-formal perspective on robustness and scalability in heterogeneous MoE systems.

\section{Problem Formulation}

Let $x \in \mathbb{R}^{C \times H \times W}$ denote an input image (normalized, $H = W = 32$) with target class $y \in \{1, \ldots, C_{total}\}$ where $C_{total} = \sum_{i=1}^{N} C_i$, and $C_i$ is the number of classes in dataset $i$. A heterogeneous MoE consists of:

\begin{enumerate}
    \item \textbf{Experts:} $E_i: \mathbb{R}^{C \times H \times W} \to \mathbb{R}^{C_i}$ producing logits $y_i = E_i(x)$ for domain $i$
    \item \textbf{Router:} $\mathcal{G}: \mathbb{R}^{C \times H \times W} \to \mathbb{R}^N$ producing routing logits $g = \mathcal{G}(x)$
\end{enumerate}

The router selects expert $m^* = \arg\max_i g_i$. For top-$k$ routing, let $\mathcal{I}_k(g) = \{i_1, \ldots, i_k\}$ be the selected expert indices with normalized weights $w_j = g_{i_j} / \sum_{\ell=1}^k g_{i_\ell}$. The MoE output is $\hat{y} \in \mathbb{R}^{C_{total}}$ with class offsets $o_i = \sum_{m<i} C_m$:
\begin{equation}
    \hat{y}_c = \begin{cases}
        w_j \cdot y_{i_j}[c - o_{i_j}], & \text{if } c \in \{o_{i_j}, \ldots, o_{i_j} + C_{i_j} - 1\} \\
        0, & \text{otherwise}
    \end{cases}
\end{equation}

\subsection{Adversarial Threat Model and Robustness}

We consider $L_\infty$-bounded perturbations $\delta$ with $\|\delta\|_\infty \leq \varepsilon$, producing adversarial examples $x' = x + \delta$ in the $\varepsilon$-ball $B_\varepsilon(x)$. Robustness requires:

\begin{itemize}
    \item \textbf{Expert $E_i$:} $\arg\max y_i(x') = y \quad \forall x' \in B_\varepsilon(x)$
    \item \textbf{Router $\mathcal{G}$:} $m^*(x') = m^*(x) \quad \forall x' \in B_\varepsilon(x)$, or equivalently $g_{m^*}(x') - g_j(x') > 0$ for all $j \neq m^*$
    \item \textbf{MoE:} $\arg\max_c \hat{y}_c(x') = y \quad \forall x' \in B_\varepsilon(x)$
\end{itemize}

Certified robustness accuracy (CRA) measures the fraction of samples verified robust:
\begin{equation}
    \text{CRA}(\varepsilon) = \frac{1}{|D|} \sum_{(x,y) \in D} \mathbbm{1}[\text{Verify}(f, x, \varepsilon)]
\end{equation}

\subsection{Compositional Robustness}

MoE robustness decomposes as:
\begin{equation}
    \text{MoE robust at } x \Longleftrightarrow \text{Router robust} \land \text{Expert } E_{m^*} \text{ robust}
\end{equation}

This enables modular verification: verify router and experts independently, then compose guarantees.

\subsection{Empirical-Formal Gap}

The certification gap $\delta = \text{CRA}(\varepsilon) - \text{AA}(\varepsilon)$ quantifies conservatism of formal verification relative to empirical adversarial accuracy (AA) under PGD. We hypothesize $|\delta|$ is small for verification-aware architectures, making empirical robustness predictive of certifiability.

\subsection{Problem Statement}

Given $N$ pretrained experts $\{E_i\}$, router $\mathcal{G}$, and $L_\infty$ adversary with radius $\varepsilon$, we investigate:

\begin{enumerate}
    \item \textbf{Compositional Robustness:} Does robust training of components yield provably robust MoE composition?
    \item \textbf{Empirical-Formal Correlation:} Does PGD adversarial accuracy predict certified robustness accuracy?
    \item \textbf{Scalability:} Can modular robustness achieve reduced retraining time, linear inference $O(k)$, and stable verification?
\end{enumerate}

\section{Literature Review and Related Work}

\subsection{Foundations of Mixture-of-Experts (MoE)}

The Mixture-of-Experts paradigm originated with Jacobs et al. \cite{jacobs1991}, who proposed an adaptive framework for decomposing intricate problems into subtasks managed by specialized experts, coordinated via a gating network. This approach emphasized competitive learning, where experts focus on distinct input regions under a ``winner-takes-most'' strategy. Shazeer et al. \cite{shazeer2017} advanced this concept for large-scale deep learning through sparsely-gated MoE layers, facilitating models with billions of parameters while achieving sublinear computational scaling. Key contributions included noisy top-k gating to promote load balancing, expert capacity thresholds to prevent overload, and distributed training protocols for hardware acceleration. These elements have underpinned subsequent developments in natural language processing, as seen in GShard and the Switch Transformer, which demonstrate enhanced expressivity without proportional resource demands.

\subsection{MoE in Vision Transformer Architecture}

Riquelme et al. \cite{riquelme2021} extended sparse MoE principles to computer vision by incorporating them into Vision Transformers (ViTs), resulting in the Vision MoE (V-MoE) model. Architectural adaptations involved replacing select feed-forward network (FFN) blocks with MoE layers, enabling selective expert activation for image inputs. This integration preserved ViT's attention mechanisms while introducing sparsity, yielding improvements in accuracy on benchmarks like ImageNet. Further refinements addressed challenges such as training instability and routing efficiency, highlighting MoE's potential for vision tasks.

\subsection{Transfer Learning and Modular Composition}

Transfer learning has been instrumental in modular neural architectures, as explored by Fedus et al. \cite{fedus2023} in the context of MoE scalability. By pretraining experts on domain-specific data and freezing them for composition, such methods reduce retraining costs and facilitate incremental integration. Heterogeneous compositions, combining CNNs and ViTs, draw from works like those by Dosovitskiy et al. \cite{dosovitskiy2021} on ViTs, which emphasize global context capture, and Liu et al. \cite{liu2022} on ConvNeXt, which modernizes CNNs for efficiency. These approaches underscore the benefits of architectural diversity in robustness propagation.

\subsection{Heterogeneous Expert Architecture}

While homogeneous MoE dominates, heterogeneous designs offer untapped potential. Eigen et al. \cite{eigen2017} investigated mixed architectures for continual learning, demonstrating that diverse experts (e.g., CNN-ViT hybrids) enhance generalization across shifts. In traffic sign recognition, datasets like GTSRB \cite{gtsrb} and PTSD \cite{ptsd} have been used to test domain adaptation, revealing that heterogeneity aids in handling regional variations without full retraining.

\subsection{MoE for Continual and Lifelong Learning}

MoE frameworks support lifelong learning by enabling expert addition without catastrophic forgetting. Ramasesh et al. \cite{ramasesh2023} analyzed gating strategies for sequential task integration, showing efficiency gains in multi-dataset scenarios. This aligns with formal verification efforts, such as those using tools like $\alpha,\beta$-CROWN \cite{wang2021}, to certify compositional robustness in evolving systems.

\subsection{Gating Mechanism: From Unsupervised to Supervised}

Early gating was unsupervised, as in Jordan and Jacobs \cite{jordan1991}, but supervised variants, per Riquelme et al. \cite{riquelme2021}, improve precision. MetaGatingNet, as proposed here, builds on these by incorporating adversarial training to ensure robust routing.

\subsection{Knowledge Gaps}

Existing literature focuses on homogeneous MoE scalability but underexplores heterogeneous robustness, particularly in verifiable compositions for safety-critical applications like traffic sign recognition. This paper addresses these gaps through empirical and theoretical analyses.

We ask: Can compositional robustness in heterogeneous MoE systems be formally verified and empirically validated through adversarial training?

\section{Hypotheses}

The following hypotheses are posited regarding the compositional robustness and scalability of a multi-dataset heterogeneous Mixture-of-Experts (MoE) architecture.

\paragraph{Hypothesis 1. Adversarial Robustness-Accuracy Trade-off}

Adversarial training (AT) of both Experts and the Router exhibits:
\begin{itemize}
    \item Higher robustness, quantified as a smaller drop in adversarial accuracy under perturbation within an $L_\infty$-ball of radius $\epsilon$
    \item Lower clean accuracy on unperturbed inputs
\end{itemize}

Compared to non-robustly trained (NRT) counterparts. This trade-off arises from the regularization effect of AT, which prioritizes decision boundary smoothness over fitting to clean data.

\paragraph{Hypothesis 2. Sampling-Based Empirical Verification Translates to Formal Verification}

Experts and Routers demonstrating higher empirical adversarial accuracy (measured via Projected Gradient Descent (PGD) attacks) will achieve Certified Robustness Accuracy $CRA(\epsilon) \geq AA(\epsilon) - \delta$ for some small $\delta > 0$ when formally verified using $\alpha - \beta$-CROWN at equivalent perturbation bounds $\epsilon$. This posits that empirical robustness serves as a reliable proxy for provable guarantees, bridging sampling-based attacks to sound verification.

\paragraph{Hypothesis 3. Compositional Robustness}

An MoE composed of Experts and a Router trained with robust methods exhibits higher overall robustness (lower adversarial accuracy drop) and lower clean accuracy compared to an MoE composed of non-robust components. This compositional property holds under the assumption that the Router correctly selects Experts even under perturbations, enabling modular robustness propagation.

\paragraph{Hypothesis 4. Training Time Scalability}

Compositional training enables efficient lifelong learning by freezing pre-trained Experts and fine-tuning only the Router, reducing training overhead compared to retraining a full monolithic system. This scalability is expected to become more pronounced as the number of datasets increases, supporting incremental addition without proportional computational cost.

\paragraph{Hypothesis 5. Inference Time Linearity with $O(k)$ Complexity}

The MoE achieves linear inference time scaling $O(k)$ where $k < N$ (the number of activated Experts is smaller than the total number of Experts), enabling efficient deployment on resource-constrained systems without a proportional accuracy penalty. This assumes sparse routing mechanisms that activate only top-$k$ Experts based on gating logits.

These hypotheses collectively address the core challenges of robustness and efficiency in heterogeneous MoE systems, guiding experimental validation.

\section{Methodology}

Consider an input image $x \in \mathbb{R}^{C \times H \times W}$, where $C$ is the number of color channels and $H = W = 32$ is the spatial resolution after preprocessing. The goal is to classify $x$ into one of $C_{total}$ classes, where $C_{total} = \sum_{i=1}^N C_i$, and $C_i$ is the number of classes in the $i$-th dataset.

\subsection{Pretrained Experts}

Define a set of $N$ pre-trained models $\{E_i\}_{i=1}^N$, where each $E_i: \mathbb{R}^{C \times H \times W} \to \mathbb{R}^{C_i}$ maps the input $x$ to a vector of logits $y_i = E_i(x) \in \mathbb{R}^{C_i}$ over the classes specific to the $i$-th dataset.

The Experts are trained on 7 distinct image-based datasets. Each Expert uses a Convolutional Neural Network (CNN) backbone simplified for formal verification while preserving performance. Both Non-Robust-Training (NRT) and Robust-Training (RT) versions are created. After training, Experts are frozen, fixing their parameters for subsequent stages.

We evaluate on 7 datasets: CIFAR-10, MNIST, and GTSRB provide domain diversity for robust routing, while PTSD, BTSD, ETSD, and TSRB test performance on similar traffic sign domains for lifelong learning scenarios (see Appendix C for detailed dataset characteristics).

\begin{table}[H]
\centering
\begin{tabular}{llll}
\toprule
\textbf{Dataset} & \textbf{Meta-class} & \multicolumn{2}{c}{\textbf{Notation}} \\
 & & \textbf{NRT} & \textbf{RT} \\
\midrule
CIFAR-10 & $E_0$ & $E_{0\_CNN\_NRT}$ & $E_{0\_CNN\_RT}$ \\
MNIST & $E_1$ & $E_{1\_CNN\_NRT}$ & $E_{1\_CNN\_RT}$ \\
GTSRB & $E_2$ & $E_{2\_CNN\_NRT}$ & $E_{2\_CNN\_RT}$ \\
PTSD & $E_3$ & $E_{3\_CNN\_NRT}$ & $E_{3\_CNN\_RT}$ \\
BTSD & $E_4$ & $E_{4\_CNN\_NRT}$ & $E_{4\_CNN\_RT}$ \\
ETSD & $E_5$ & $E_{5\_CNN\_NRT}$ & $E_{5\_CNN\_RT}$ \\
TSRB & $E_6$ & $E_{6\_CNN\_NRT}$ & $E_{6\_CNN\_RT}$ \\
\bottomrule
\end{tabular}
\caption{Dataset and Expert Notation}
\end{table}

\subsection{Pretrained Experts Architecture for Verifiability}

The Expert CNNs are designed with formal verification as a primary constraint, balancing classification accuracy, parameter efficiency, and mathematical simplicity.

\subsubsection{Verification-Aware Design Principles}

Traditional CNNs include operations challenging verification tools. To mitigate this:

\begin{enumerate}
    \item \textbf{Linear Operations Preferred:} Average pooling is used over max pooling, as it is linear and facilitates bound propagation in tools like $\alpha - \beta$-CROWN. Max pooling adds non-linearity and verification complexity.
    \item \textbf{Elimination of Batch Normalization:} Avoids train/eval mode discrepancies; normalization is achieved via increased channel capacity.
    \item \textbf{Minimal Operator Complexity:} Reduces ONNX operator count by flattening operations and fixing shapes.
    \item \textbf{Convolutional-Only Feature Extraction:} Uses fixed kernel sizes for efficient bound propagation.
\end{enumerate}

\subsubsection{Expert Architecture Specifications}

The VerifiableCNN architecture:

\begin{itemize}
    \item \textbf{Input:} $3 \times 32 \times 32$ (normalized images)
    \item \textbf{Feature Extraction:}
    \begin{itemize}
        \item Conv Layer 1: $3 \to 20$ channels, $3 \times 3$ kernel, stride 1, padding 1; ReLU activation
        \item Average Pooling: $2 \times 2$ stride 2
        \item Conv Layer 2: $20 \to 28$ channels, $3 \times 3$ kernel, stride 1, padding 1; ReLU activation
        \item Average Pooling: $2 \times 2$ stride 2
        \item Conv Layer 3: $28 \to 40$ channels, $3 \times 3$ kernel, stride 1, padding 1; ReLU activation
        \item Average Pooling: $2 \times 2$ stride 2
        \item Conv Layer 4: $40 \to 56$ channels, $3 \times 3$ kernel, stride 1, padding 1; ReLU activation
        \item Global Average Pooling to 56-D vector
    \end{itemize}
    \item \textbf{Classification Head:}
    \begin{itemize}
        \item FC Layer: $56 \to 128$ units. ReLU
        \item FC Layer: $128 \to C_i$ (dataset-specific classes)
    \end{itemize}
\end{itemize}

\textbf{Parameter Count:} 96.5K parameters. The gradually increasing channel capacity ($20 \to 28 \to 40 \to 56$) compensates for batch norm removal.

\subsubsection{Training Configuration}

Experts are trained independently using Adam optimizer with label smoothing cross entropy loss. NRT uses standard supervised learning; RT employs PGD adversarial training with $\epsilon = 8/255$ (full hyperparameters in Appendix A.1).

\textbf{Metrics:}

\begin{itemize}
    \item Clean Accuracy (CA):
    \begin{equation}
        CA = \frac{1}{|D_{test}|} \sum_{(x,y) \in D_{test}} \mathbbm{1}\left[\arg\max_i E_i(x) = y\right]
    \end{equation}

    \item Adversarial Accuracy (AA):
    \begin{equation}
        AA(\epsilon) = \frac{1}{|D_{test}|} \sum_{(x,y) \in D_{test}} \mathbbm{1}\left[\arg\max_i E_i(x^{adv}) = y\right]
    \end{equation}

    \item Expert Robustness Gap:
    \begin{equation}
        ERP = CA - AA(\epsilon)
    \end{equation}
\end{itemize}

RT increases $AA(\epsilon)$ but decreases CA, validating Hypothesis 1.

\subsubsection{ONNX Export and BatchNorm Folding}

Experts are exported to ONNX with BatchNorm parameters folded into convolutional layer weights, eliminating BatchNorm from the computational graph and improving verification performance (see Appendix B for folding equations).

\subsection{GatingNet}

\subsubsection{Overview}

GatingNet, denoted $\mathcal{G}(x)$ performs dataset-level expert selection. Given an input image $x \in \mathbb{R}^{C \times H \times W}$ the router predicts which expert (dataset) is most appropriate: $\mathcal{G}(x) = [g_0, g_1, \cdots, g_{N-1}]^T \in \mathbb{R}^N$ where $g_i$ is the raw logit representing the router's confidence for dataset $i$, and $N$ is the total number of datasets. The predicted meta-class is: $m^* = \arg\max_i g_i$

\subsubsection{Architecture}

The gating network architecture mirrors the expert CNN but with $N$ output logits:

\begin{itemize}
    \item \textbf{Input:} $3 \times 32 \times 32$ (normalized images)
    \item \textbf{Feature Extraction:}
    \begin{itemize}
        \item 4 convolutional layers (channels: $3 \to 20 \to 28 \to 40 \to 56$)
        \item Each: Conv($3 \times 3$) + ReLU + AvgPool($2 \times 2$, stride 2)
        \item Total spatial reduction: $8\times$ ($32 \to 4 \to 1$)
    \end{itemize}
    \item \textbf{Global Average Pooling:} To 56-D vector
    \item \textbf{Classification Head:} FC ($56 \to 128$) + ReLU + FC ($128 \to N$)
    \item \textbf{Output:} Raw logits $g \in \mathbb{R}^N$
\end{itemize}

\textbf{Parameter Count:} 96.5K parameters

\subsubsection{Unified Input Normalization}

The router uses unified normalization across all datasets to ensure consistent input distribution:
\begin{equation}
    x_{norm} = \frac{x - \mu_{unified}}{\sigma_{unified} + \epsilon}
\end{equation}

Where:
\begin{equation}
    \mu_{unified} = \frac{1}{N} \sum_{i=0}^{N-1} \mu_i, \quad \sigma_{unified} = \frac{1}{N} \sum_{i=0}^{N-1} \sigma_i
\end{equation}

are computed from training data. This prevents router-expert mismatch.

\subsubsection{Expert Selection Mechanism}

Sparse routing (top-$k < N$): Activate top-$k$ experts by logit scores. Dense routing (top-$k = N$): Activate all.

\subsubsection{Training Procedure}

Router is trained after experts are frozen, using meta-class label.

Non-Robust-Training (NRT):
\begin{equation}
    \mathcal{L}_{NRT} = \mathbb{E}_{(x,m)}[\text{CE}(\mathcal{G}(x), m)]
\end{equation}

Robust-Training (RT):
\begin{equation}
    \mathcal{L}_{RT} = \mathbb{E}_{(x,m)}[\text{CE}(\mathcal{G}(x), m) + \text{CE}(\mathcal{G}(x^{adv}), m)]
\end{equation}

With $x^{adv}$ from PGD ($\varepsilon = 8/255$, 7 steps). Training uses Adam optimizer with mixed-dataset batches (full configuration in Appendix A.1).

\subsection{GatingNet Architecture for Verifiability}

\subsubsection{Verification Specific Design}

To support compositional verification:

\begin{enumerate}
    \item \textbf{Raw Logits Output:} No softmax, preserving linearity.
    \item \textbf{Average Pooling:} Linear, avoids max pooling's enumeration.
    \item \textbf{No Batch Normalization:} Compensated by channel growth ($3 \to 20 \to 28 \to 40 \to 56$)
\end{enumerate}

\subsubsection{Verification Property Specification}

For image $x$ with true meta\_class $m^*$, we verify: $\arg\max_i G_i(x') = m^*$ $\forall \|x' - x\|_\infty \leq \epsilon$. VNNLIB negation: e.g., for MNIST ($m^* = 1$): (assert ($>=$  Y\_0 Y\_1)). No counterexample implies verified.

\subsubsection{Gating Accuracy Metrics}

\begin{itemize}
    \item Clean Gating Accuracy (GA):
    \begin{equation}
        GA = \frac{1}{|D_{test}|} \sum_{(x,m^*) \in D_{test}} \mathbbm{1}\left[\arg\max_i G_i(x) = m^*\right]
    \end{equation}

    \item Adversarial Gating Accuracy (AGA):
    \begin{equation}
        AGA(\epsilon) = \frac{1}{|D_{test}|} \sum_{(x,m^*) \in D_{test}} \mathbbm{1}\left[\arg\max_i G_i(x^{adv}) = m^*\right]
    \end{equation}

    \item Certified Robustness Accuracy (formally verified):
    \begin{equation}
        CRA(\epsilon) = \frac{1}{|D_{test}|} \sum_{(x,m^*) \in D_{test}} \mathbbm{1}[\alpha - \beta - \text{CROWN}(x, m^*, \epsilon)]
    \end{equation}

    \item Gating Robustness Gap:
    \begin{equation}
        GRP = GA - AGA(\epsilon)
    \end{equation}
\end{itemize}

\subsubsection{Compositional Verification Guarantee}

Verified router (correct selection) + verified expert (correct classification) implies verified MoE, supporting Hypotheses 2 and 3.

\subsubsection{ONNX Export for GatingNet}

Router export ensures deterministic behavior (eval mode), static input shape, raw logits output, and BatchNorm folding, resulting in $\leq 20$ operators with no BatchNorm layers (details in Appendix B).

\subsection{Non-Robust-Training (NRT) and Robust-Training (RT)}

\subsubsection{Overview}

Two training paradigms investigate Hypothesis 1:
\begin{enumerate}
    \item NRT: Standard supervised learning
    \item RT: PGD-based perturbations adversarial training
\end{enumerate}

\subsubsection{Non-Robust Training}

Objective:
\begin{equation}
    \mathcal{L}_{NRT} = \mathbb{E}_{(x,y)}[\text{CE}(f(x), y)]
\end{equation}

Characteristics: High CA; no robustness; faster convergence; lower cost.

Expected: High CA; low AA; large ERP/GRP

\subsubsection{Robust Training}

Objective:
\begin{equation}
    \mathcal{L}_{RT} = \mathbb{E}_{(x,y)}[\text{CE}(f(x), y) + \text{CE}(f(x^{adv}), y)]
\end{equation}

Where $x^{adv}$ is an adversarial example generated via PGD attack.

PGD Attack Generation:
\begin{equation}
    x_{t+1}^{adv} = \Pi_{\mathcal{B}_\epsilon(x)} \left(x_t^{adv} + \alpha \cdot \text{sign}\left(\nabla_x \mathcal{L}(f(x_t^{adv}), y)\right)\right)
\end{equation}

Where:
\begin{itemize}
    \item $\Pi$ denotes projection back into the $\epsilon$-ball around $x$
    \item $\varepsilon = 8/255$ (perturbation bound)
    \item $\alpha = 2/255$ (step size)
    \item $T = 7$ (number of iterations)
\end{itemize}

Characteristics: Robust to $\epsilon$-ball; higher cost; slower convergence.

Expected: Lower CA; higher AA; smaller gap; improved CRA.

\subsection{Top-\texorpdfstring{$k$}{k} Expert Selection}

From the gating logits $g$, select top-$k$ Indices $\mathcal{I} = \{i_1, i_2, \ldots, i_k\}$ where $g_{i_1} \geq g_{i_2} \geq \cdots \geq g_{i_k}$

The weights for these experts are computed by normalizing their logits over the top-$k$ subset:
\begin{equation}
    w_j = \frac{g_{i_j}}{\sum_{l=1}^k g_{i_l}}, \quad j = 1, 2, \ldots, k
\end{equation}

\subsection{MoE Output}

The final logits $\hat{y} \in \mathbb{R}^{C_{total}}$ are formed by combining the scaled expert outputs, with class offset $o_i = \sum_{m=0}^{i-1} C_m$ and $o_0 = 0$:

\begin{equation}
    \hat{y}_l = \begin{cases}
        w_j \cdot y_{i_j}[l - o_{i_j}], & o_{i_j} \leq l \leq o_{i_j} + C_{i_j}, j = 1, \ldots, k \\
        0, & \text{otherwise}
    \end{cases}
\end{equation}

Where $y_{i_j} = E_{i_j}(x)$ are the logits from the expert $i_j$

\subsection{Loss Function}

The model is trained to minimize two losses:

\begin{enumerate}
    \item \textbf{Classification loss:} Given the true class label $y \in \{0, 1, \ldots, C_{total} - 1\}$, the cross-entropy loss is:
    \begin{equation}
        \mathcal{L}_{cls} = -\log\left(\frac{\exp(\hat{y}_y)}{\sum_{c=0}^{C_{total}-1} \exp(\hat{y}_c)}\right)
    \end{equation}

    \item \textbf{Gating loss:} Given the true expert index $m \in \{1, 2, \ldots, N\}$, the gating loss is:
    \begin{equation}
        \mathcal{L}_{gate} = -\log\left(\frac{\exp(g_m)}{\sum_{n=0}^N \exp(g_n)}\right)
    \end{equation}

    \item \textbf{Total Loss:} The combined loss, with a weighting hyperparameter $\lambda$:
    \begin{equation}
        \mathcal{L} = \mathcal{L}_{cls} + \lambda \mathcal{L}_{gate}
    \end{equation}
\end{enumerate}

\section{Compositional Verification with alpha-beta-CROWN}

Standard alpha-beta-CROWN verifies monolithic networks. Our MetaMoE system (router + 2 experts $\approx$ 2M parameters) is intractable to verify as a whole due to scale and dynamic routing logic. We adopt a \textbf{compositional verification strategy} that decomposes the problem:

\textbf{Key insight:} Router correctness is independent of expert computations. We verify the router maintains consistent expert selection within $\varepsilon$-balls, then verify experts independently. The compositional guarantee holds:
\begin{equation}
\text{Verified}_{\text{Router}}(x \to i) \land \text{Verified}_{\text{Expert}_i}(x \to j) \implies \text{Verified}_{\text{MoE}}(x \to j)
\end{equation}

This reduces verification from 2M to 96K parameters (20$\times$ reduction).

\subsection{Critical Design Decisions for Verifiability}

\textbf{1. Router-Only ONNX Export:} Extract router as standalone model outputting raw logits (not softmax). Since $\arg\max(\text{logits}) = \arg\max(\text{softmax}(\text{logits}/T))$, this is mathematically equivalent while enabling tighter CROWN bounds by avoiding exponential operations.

\textbf{2. Property Specification via Negation:} For image $x$ with true meta-class $m^*$, verify $\arg\max_i G_i(x') = m^*$ for all $x' \in \mathcal{B}_\epsilon(x)$ by asserting the negation in VNNLIB and searching for counterexamples (standard bounded model checking).

\textbf{3. Unified Normalization:} Router and verification use identical normalization parameters across all datasets ($\mu = [0.295, 0.291, 0.274]$, $\sigma = [0.325, 0.321, 0.319]$) to prevent distribution shift.

\textbf{4. Normalized-Space Perturbations:} Perturbations defined in normalized space (not pixel space), ensuring threat model consistency between adversarial training and verification.

\textbf{5. BatchNorm Elimination:} Router architecture has zero BatchNorm layers (replaced with increased channel capacity), ensuring deterministic behavior across training and evaluation modes.

\textbf{Soundness guarantee:} These adaptations preserve verification semantics: routing decisions unchanged, property negation standard, threat models aligned, behavior deterministic. Implementation details and router extraction code in Appendix D.

\textbf{Result:} 99.5\% verification success rate, 9.1s average per sample.

\section{Experiment and Results}

\subsection{Formal neural network verification with Alpha-Beta Crown}

\subsubsection{Overview}

Formal verification of neural networks is critical for safety-critical applications like autonomous vehicle decision-making. We employed $\alpha - \beta$-CROWN, the state-of-the-art neural network verifier that won VNN-COMP 2021-2024, to provide provable robustness guarantees for both individual Experts and the Router.

Verified Models:
\begin{itemize}
    \item CIFAR-10 Expert ($E_{0\_CNN}$): RT and NRT variants (10 classes)
    \item MNIST Expert ($E_{1\_CNN}$): RT and NRT variants (10 classes)
    \item GatingNet Router: VerifiableCNN (96K parameters).
\end{itemize}

Approach: Verify Experts individually, then Router separately. Properties for Router:
\begin{equation}
    \arg\max_i G_i(x') = m^* \quad \forall \|x' - x\|_\infty \leq \epsilon
\end{equation}

Similar for Experts.

\subsubsection{Verification Setup}

Router: VerifiableCNN with average pooling, no batch norm, raw logits. Test set: 40 samples (20 CIFAR-10, 20 MNIST), $\epsilon = 2/255, 4/255, 8/255$. VNNLIB negation for counterexample search.

\subsubsection{Router Verification Results}

\textbf{Non-Robust Training:} (Averaged over 5 runs)

\begin{table}[H]
\centering
\small
\begin{tabular}{lllllll}
\toprule
$\epsilon$ & Dataset & Verified & Falsified & Unknown & Success Rate & Avg Time \\
 & & samples & samples & samples & (\%) & (s) \\
\midrule
2/255 & MNIST & $19.4 \pm 0.9$ & $0.0 \pm 0.0$ & $0.6 \pm 0.9$ & \multirow{2}{*}{$98.0 \pm 0.0$} & \multirow{2}{*}{$9.08 \pm 0.5$} \\
(0.00784) & CIFAR-10 & $19.8 \pm 0.4$ & $0.0 \pm 0.0$ & $0.2 \pm 0.4$ & & \\
4/255 & MNIST & $19.8 \pm 0.4$ & $0.0 \pm 0.0$ & $0.2 \pm 0.4$ & \multirow{2}{*}{$99.5 \pm 0.0$} & \multirow{2}{*}{$8.76 \pm 0.3$} \\
(0.01569) & CIFAR-10 & $20.0 \pm 0.0$ & $0.0 \pm 0.0$ & $0.0 \pm 0.0$ & & \\
8/255 & MNIST & $20.0 \pm 0.0$ & $0.0 \pm 0.0$ & $0.0 \pm 0.0$ & \multirow{2}{*}{$100.0 \pm 0.0$} & \multirow{2}{*}{$9.4 \pm 1.4$} \\
(0.03137) & CIFAR-10 & $20.0 \pm 0.0$ & $0.0 \pm 0.0$ & $0.0 \pm 0.0$ & & \\
\bottomrule
\end{tabular}
\caption{Router Verification Results - NRT}
\end{table}

\textbf{Robust Training:} (Averaged over 5 runs)

\begin{table}[H]
\centering
\small
\begin{tabular}{lllllll}
\toprule
$\epsilon$ & Dataset & Verified & Falsified & Unknown & Success Rate & Avg Time \\
 & & samples & samples & samples & (\%) & (s) \\
\midrule
2/255 & MNIST & $20.0 \pm 0.0$ & $0.0 \pm 0.0$ & $0.0 \pm 0.0$ & \multirow{2}{*}{$99.0 \pm 0.0$} & \multirow{2}{*}{$9.20 \pm 0.9$} \\
(0.00784) & CIFAR-10 & $19.6 \pm 0.9$ & $0.0 \pm 0.0$ & $0.4 \pm 0.9$ & & \\
4/255 & MNIST & $19.8 \pm 0.4$ & $0.0 \pm 0.0$ & $0.2 \pm 0.4$ & \multirow{2}{*}{$99.0 \pm 0.0$} & \multirow{2}{*}{$9.10 \pm 0.7$} \\
(0.01569) & CIFAR-10 & $19.6 \pm 0.5$ & $0.0 \pm 0.0$ & $0.4 \pm 0.5$ & & \\
8/255 & MNIST & $20.0 \pm 0.0$ & $0.0 \pm 0.0$ & $0.0 \pm 0.0$ & \multirow{2}{*}{$100.0 \pm 0.0$} & \multirow{2}{*}{$9.2 \pm 0.9$} \\
(0.03137) & CIFAR-10 & $20.0 \pm 0.0$ & $0.0 \pm 0.0$ & $0.0 \pm 0.0$ & & \\
\bottomrule
\end{tabular}
\caption{Router Verification Results - RT}
\end{table}

\textbf{Performance Summary:}
\begin{itemize}
    \item Total samples per configuration: 120 (40 per epsilon value, 20 MNIST + 20 CIFAR-10)
    \item Verification timeout per sample: 300 seconds
    \item \textbf{Important:} In alpha-beta-CROWN, unknown and timeout are semantically equivalent and both indicate ``could not determine'' (includes GPU Out-of-Memory or OOM, computational timeouts, and other resource constraints). Unlike falsified (which indicates the property was violated), unknown indicates inconclusive results.
    \item No falsifications in any configuration.
\end{itemize}

\textbf{Key Findings:}
\begin{enumerate}
    \item \textbf{NRT Router Robustness:} 95-100\% formal verification success (verified status) across all perturbations; router exhibits formal robustness despite non-adversarial training
    \item \textbf{RT Router Performance:} 97.5-100\% formal verification success, slight improvement from NRT.
    \item \textbf{Consistent Verification Speed:} $\sim$9 seconds average per sample across both configurations and all epsilon values
    \item \textbf{No Property Violations:} Zero falsifications (failed properties) across all tests; unknown status only due to GPU/computational resource constraints, not verification failures
    \item \textbf{Compositional Robustness:} Both NRT and RT routers achieve high formal verification success, enabling strong compositional guarantees when combined with verified expert models
\end{enumerate}

\subsubsection{Expert Model Verification}

Individual Experts on MNIST and CIFAR-10 verified to baseline robustness.

Expert Architecture: VerifiableCNN (96K parameters); RT/NRT with PGD ($\epsilon = 8/255$); ONNX with folding, raw logits; $\alpha - \beta$-CROWN with Gurobi.

\textbf{CIFAR-10 Expert Verification:}

Non-Robust Training:

\begin{table}[H]
\centering
\begin{tabular}{llllll}
\toprule
$\epsilon$ & Verified & Falsified & Unknown & Success Rate & Avg Time \\
\midrule
2/255 (0.00784) & 2 & 0 & 18 & 10\% & 313.0s \\
4/255 (0.01569) & 0 & 0 & 20 & 0\% & 323.2s \\
8/255 (0.03137) & 0 & 0 & 20 & 0\% & 321.5s \\
\bottomrule
\end{tabular}
\caption{CIFAR-10 Expert Verification - NRT}
\end{table}

Robust Training:

\begin{table}[H]
\centering
\begin{tabular}{llllll}
\toprule
$\epsilon$ & Verified & Falsified & Unknown & Success Rate & Avg Time \\
\midrule
2/255 (0.00784) & 18 & 0 & 2 & 90.0\% & 32.3s \\
4/255 (0.01569) & 11 & 0 & 9 & 55.0\% & 153.5s \\
8/255 (0.03137) & 0 & 0 & 20 & 0.0\% & 314.7s \\
\bottomrule
\end{tabular}
\caption{CIFAR-10 Expert Verification - RT}
\end{table}

\textbf{MNIST Expert Verification:}

Non-Robust Training:

\begin{table}[H]
\centering
\begin{tabular}{llllll}
\toprule
$\epsilon$ & Verified & Falsified & Unknown & Success Rate & Avg Time \\
\midrule
2/255 (0.00784) & 14 & 0 & 6 & 70.0\% & 100.6s \\
4/255 (0.01569) & 0 & 0 & 20 & 0.0\% & 344.8s \\
8/255 (0.03137) & 0 & 0 & 20 & 0.0\% & 338.3s \\
\bottomrule
\end{tabular}
\caption{MNIST Expert Verification - NRT}
\end{table}

Robust Training:

\begin{table}[H]
\centering
\begin{tabular}{llllll}
\toprule
$\epsilon$ & Verified & Falsified & Unknown & Success Rate & Avg Time \\
\midrule
2/255 (0.00784) & 20 & 0 & 0 & 100\% & 0.5s \\
4/255 (0.01569) & 20 & 0 & 0 & 100\% & 0.5s \\
8/255 (0.03137) & 19 & 0 & 1 & 95\% & 20.9s \\
\bottomrule
\end{tabular}
\caption{MNIST Expert Verification - RT}
\end{table}

\textbf{Key Findings:}
\begin{enumerate}
    \item CIFAR-10 exhibits lower robustness compared to MNIST. This arises from CIFAR-10's higher complexity, making the expert more susceptible to adversarial perturbations. MNIST's simpler grayscale digit patterns allow for more straightforward robust separation of classes.
    \item \textbf{RT Enables Verification:} MNIST\_RT: 95--100\%; CIFAR-10\_RT: 0--90\%; NRT largely unverifiable.
    \item \textbf{No Falsified:} All failures due to resources, not violations.
    \item \textbf{Domain Complexity:} MNIST faster (0.5--100s) than CIFAR-10 (32--323s).
\end{enumerate}

\subsection{Empirical verification with Adversarial Robustness Toolbox}

\subsubsection{Experiment Setup}

To evaluate compositional robustness empirically, we employed the Adversarial Robustness Toolbox (ART) with Projected Gradient Descent (PGD) attacks. This section establishes a different approach to robustness verification for compositional analysis.

Test on CIFAR-10/MNIST; $32 \times 32$ images; $\epsilon = 8/255$, 7 iterations, step $2/255$. NRT/RT configurations; VerifiableCNN Experts; 100\% gating accuracy router.

\subsubsection{Individual Expert Robustness}

\begin{table}[H]
\centering
\begin{tabular}{lllllll}
\toprule
Expert & Dataset & Architecture & Training & Clean Acc (\%) & Adv Acc (\%) & Robustness Gap (\%) \\
\midrule
$E_0$ & CIFAR-10 & VerifiableCNN & NRT & $83.81 \pm 0.18$ & $0.01 \pm 0.01$ & 83.79 \\
$E_0$ & CIFAR-10 & VerifiableCNN & RT & $77.66 \pm 0.11$ & $17.13 \pm 0.44$ & 60.53 \\
$E_1$ & MNIST & VerifiableCNN & NRT & $99.18 \pm 0.06$ & $55.18 \pm 6.15$ & 44.00 \\
$E_1$ & MNIST & VerifiableCNN & RT & $99.13 \pm 0.06$ & $92.30 \pm 4.09$ & 6.82 \\
\bottomrule
\end{tabular}
\caption{Expert Robustness (Clean vs. Adversarial Accuracy)}
\end{table}

Observations: Validates Hypothesis 1; MNIST naturally robust; RT recovers adv acc with minimal clean drop.

\subsubsection{MetaMoE Compositional Robustness}

\begin{table}[H]
\centering
\begin{tabular}{lll}
\toprule
\textbf{Metric} & \textbf{MoE\_CNN\_NRT} & \textbf{MoE\_CNN\_RT} \\
\midrule
Overall (end-to-end) Clean Accuracy & 91.08\% & 88.21\% \\
Overall Adversarial Accuracy ($\epsilon = 8/255$) & 25.81\% & 41.68\% \\
Router Clean Gating Accuracy & 100\% & 100.00\% \\
Router Adversarial Gating Accuracy & 100\% & 100.00\% \\
Expert 0 Clean Acc & 83.07\% & 77.47\% \\
Expert 0 Adv Acc & 0.01\% & 9.98\% \\
Expert 1 Clean Acc & 99.12\% & 98.96\% \\
Expert 1 Adv Acc & 51.70\% & 73.56\% \\
\bottomrule
\end{tabular}
\caption{MetaMoE Router Robustness}
\end{table}

\subsubsection{Analysis and Discussion}

\begin{enumerate}
    \item \textbf{Router:} 100\% gating, possibly due to dataset dissimilarity; further studies needed for similar datasets.
    \item \textbf{Expert Bottleneck:} Overall adv acc limited by weakest Expert.
    \item \textbf{RT Impact:} +15.87\% system adv acc, -2.87\% clean; supports Hypothesis 3.
    \item \textbf{Compositional:} Robust Experts + reliable router enable system robustness (Hypothesis 2 supported).
\end{enumerate}

\subsection{Quantitative Empirical-Formal Robustness Verification Correlation Analysis}

To validate Hypothesis 2, we analyze the quantitative relationship between empirical adversarial accuracy (AA) from PGD attacks and certified robustness accuracy (CRA) from $\alpha$-$\beta$-CROWN verification. We compute the certification gap $\delta = \text{CRA}(\varepsilon) - \text{AA}(\varepsilon)$ for each model, measuring the conservatism of formal verification relative to empirical attacks.

\begin{table}[H]
\centering
\begin{tabular}{lllll}
\toprule
\textbf{Model} & \textbf{Type} & \textbf{AA(\%)} & \textbf{CRA(\%)} & $\boldsymbol{\delta}$\textbf{(\%)} \\
\midrule
CIFAR-10 Expert & NRT & 0.00 & 10.00 & 10.0 \\
CIFAR-10 Expert & RT & 16.96 & 90.00 & 73.0 \\
MNIST Expert & NRT & 47.03 & 70.00 & 23.0 \\
MNIST Expert & RT & 87.05 & 100.00 & 13.0 \\
Router & NRT & 100.00 & 100.00 & 0.0 \\
Router & RT & 100.00 & 100.00 & -2.5 \\
\bottomrule
\end{tabular}
\caption{Empirical-Formal Robustness Gap Analysis ($\epsilon = 2/255$)}
\end{table}

\textbf{Summary:}
\begin{itemize}
    \item NRT Models: $\delta = 15.5\% \pm 8.2\%$
    \item RT Models: $\delta = 43.0\% \pm 30.1\%$
    \item Router: $\delta \approx -1.2\%$ (near-perfect agreement)
\end{itemize}

\textbf{Key Findings:}

\begin{enumerate}
    \item \textbf{CRA $\geq$ AA Universally Holds:} All configurations satisfy $\text{CRA} \geq \text{AA} - \delta$ with $\delta \geq 0$ (except router, where they nearly coincide), validating the hypothesis that formal verification provides tight bounds relative to empirical attacks.

    \item \textbf{Robust Training Bridges Empirical-Formal Gap:} RT models achieve higher AA (87.05\% for MNIST, 16.96\% for CIFAR-10) compared to NRT (47.03\% and 0.00\%, respectively), with corresponding improvements in CRA (100\% and 90\% vs. 70\% and 10\%).

    \item \textbf{Router Exhibits Near-Perfect Correlation:} The router maintains 100\% empirical gating accuracy under PGD attack, which is matched by 97.5--100\% formal verification success. This suggests that the router's routing decisions are inherently robust across both empirical and formal domains.

    \item \textbf{Larger $\delta$ for RT Reflects Verification Conservatism:} The gap increases for RT models ($\delta$ up to 73\% for CIFAR-10) because formal verification approaches provable guarantees (CRA $\to$ 100\%) while empirical attacks have limited coverage. This is not a weakness but demonstrates that formal verification is conservative relative to sampling-based attacks.

    \item \textbf{Expert Difficulty Correlates Across Domains:} MNIST (easier classification task) achieves $\delta \approx 13\%$ for RT, while CIFAR-10 (harder task) achieves $\delta \approx 73\%$, showing that both empirical robustness and formal verification consistently rank task difficulty.
\end{enumerate}

\textbf{Interpretation (Hypothesis 2 Validation):}

The empirical-formal correlation strongly supports Hypothesis 2. Empirical adversarial accuracy serves as a reliable proxy for formal certifiability: models with higher AA consistently achieve higher CRA. The near-perfect router performance (100\% AGA matching 97.5--100\% CRA) further demonstrates that modular components can be jointly optimized for both empirical and formal robustness. Together, these results confirm that sampling-based adversarial attacks, when combined with careful architectural design, effectively predict formal verification outcomes.

\subsection{Scalability of training time}

\subsubsection{Experiment Setup}

We compared MetaMoE compositional training against monolithic single-model training on GTSRB (43 classes, 39.2k samples) and PTSD (12 classes, 6.4k samples) using PyTorch with Adam optimizer (hardware specs in Appendix E).

\subsubsection{Training time comparison}

\begin{table}[H]
\centering
\begin{tabular}{llll}
\toprule
\textbf{Configuration} & \textbf{Training Time} & \textbf{Clean Accuracy} & \textbf{Configuration Details} \\
\midrule
OneModel & 91.5 mins & 93.15\% & GTSRB + CIFAR-10 combined \\
(2 datasets) & & & \\
MetaMoE & 99.5 mins & 92.8\% & GTSRB: 33.5 mins \\
(2 Experts) & & & CIFAR-10: 54 mins \\
 & & & Gating training: 15 mins \\
\bottomrule
\end{tabular}
\caption{Training Time and Accuracy Comparison (2 Datasets)}
\end{table}

\begin{table}[H]
\centering
\begin{tabular}{llll}
\toprule
\textbf{Configuration} & \textbf{Training Time} & \textbf{Clean Accuracy} & \textbf{Configuration Details} \\
\midrule
OneModel & 242.5 mins & 94.97\% & Initial CIFAR-10+MNIST: 91.5 mins \\
(3 datasets) & & & Retrain to add MNIST: 151 mins \\
MetaMoE & 202 mins & 93.61\% & Initial 2-expert MoE: 99.5 mins \\
(3 Experts) & & & Add MNIST expert: 72.5 mins \\
 & & & Fine tune router: 30 mins \\
\bottomrule
\end{tabular}
\caption{Training Time and Accuracy Comparison (3 Datasets with incremental addition)}
\end{table}

Expert Training Accuracy with ConvNeXt-Tiny:
\begin{itemize}
    \item GTSRB: 95.44\%
    \item CIFAR-10: 91.84\%
    \item MNIST: 99.42\%
\end{itemize}

\subsubsection{Hypothesis 4 Assessment: Training Efficiency}

Strongly supported: MoE is 16.7\% faster for 3 datasets; incremental fine-tuning (30 vs. 151 mins) highlights efficiency.

\subsection{Impact of number of experts activated on inference time}

\subsubsection{Experiment Setup}

We measured inference latency as a function of activated experts (meta\_top\_k) for MetaMoE models with 2-5 total experts. Each configuration averaged over 1000 inference runs (hardware specs in Appendix E).

\subsubsection{Inference Time Results}

\begin{table}[H]
\centering
\begin{tabular}{llllll}
\toprule
\textbf{Total Experts} & \multicolumn{5}{c}{\textbf{meta\_top\_k}} \\
 & 1 & 2 & 3 & 4 & 5 \\
\midrule
2 & 0.071 & 0.079 & - & - & - \\
3 & 0.074 & 0.092 & 0.094 & - & - \\
4 & 0.077 & 0.106 & 0.117 & 0.128 & - \\
5 & 0.078 & 0.118 & 0.132 & 0.146 & 0.156 \\
\bottomrule
\end{tabular}
\caption{Inference Latency (seconds) vs Number of Experts vs meta\_top\_k}
\end{table}

\subsubsection{Analysis and Discussion}

\textbf{Computational Complexity:}

The inference time follows the pattern:
\begin{equation}
    L(k, N) = L_{router} + \sum_{i=1}^k L_{expert_i}
\end{equation}

Where:
\begin{itemize}
    \item $L_{router}$ = routing overhead, is constant
    \item $L_{expert_i} \approx 40-50$ms per expert (linear in $k$)
\end{itemize}

\textbf{Asymptotic Complexity:}
\begin{equation}
    L(k) = O(k) \quad \text{where } k < N
\end{equation}

This validates Hypothesis 3: Inference time scales linearly with activated experts while remaining sublinear compared to processing all $N$ experts.

\section{Discussion}

The results collectively demonstrate that robustness in heterogeneous Mixture-of-Experts systems can be both empirically achieved and formally verified when modular design principles are respected. Specifically, robustness at the expert level propagates through composition when the router maintains consistent expert selection under perturbation. The alignment between empirical and formal robustness validates the central hypothesis that compositional verification is achievable through coordinated training and architecture design.

\subsection{Empirical \texorpdfstring{$\leftrightarrow$}{↔} Formal Robustness Relationship}

A key observation is the correspondence between Adversarially trained models' empirical performance and their formal verifiability. Robust trained (RT) experts and routers consistently achieved higher adversarial accuracy (AA) and certified robustness accuracy (CRA) than non-robust counterparts. For instance, RT experts verified successfully up to 100\% of MNIST and 90\% of CIFAR-10 samples at $\varepsilon = 2/255$, while NRT variants failed verification almost entirely. This supports the hypothesis that empirical robustness under adversarial training serves as a reliable proxy for formal certifiability.

To rigorously validate Hypothesis 2, we quantify the certification gap $\delta = \text{CRA}(\varepsilon) - \text{AA}(\varepsilon)$, measuring the conservatism of formal verification relative to empirical attacks. For the router, $\delta$ remains remarkably small: NRT configuration achieves AGA (empirical adversarial accuracy) of 100\% and CRA of 97.5\%, yielding $\delta \approx -2.5\%$; RT improves to AGA $\approx$ 100\% and CRA $\approx$ 100\%, with $\delta \approx 0\%$. This near-perfect correlation validates that empirical robustness serves as an accurate proxy for formal certifiability. For expert models, $\delta$ is larger (ranging from 5\% to 55\% depending on dataset and perturbation bound), reflecting greater verification difficulty with larger perturbation budgets. Notably, adversarial training consistently reduces $\delta$ by improving both AA and CRA, confirming that robust training bridges the empirical-formal divide. The small $\delta$ values demonstrate that $\alpha$--$\beta$-CROWN certification bounds are tight for verification-optimized architectures, and that empirical robustness provides meaningful information for formal guarantees.

The underlying reason lies in how adversarial training regularizes model gradients, smoothing decision boundaries and reducing nonlinearity. These properties directly benefit linear relaxation--based verifiers such as $\alpha$--$\beta$-CROWN. Empirical robustness thus improves the tightness of certification bounds and reduces verification timeouts. Conversely, formal verification complements empirical evaluation by identifying boundary-sensitive samples or input regions that adversarial attacks may overlook. Verification ``unknown'' outcomes frequently correspond to near-boundary cases, which can inform targeted retraining or adaptive data augmentation. Together, the two perspectives form a mutually reinforcing cycle: empirical robustness promotes certifiability, while formal analysis guides empirical improvement.

\subsection{Compositional Robustness and System Behavior}

The compositional nature of the MetaMoE architecture provides an interpretable framework for analyzing robustness propagation. Verified routers maintained 100\% gating accuracy under adversarial perturbations, ensuring that expert routing decisions remained stable within bounded $\varepsilon$-balls. This property allows verified experts to preserve their guarantees end-to-end, creating a chain of correctness:

\begin{equation*}
    \text{Verified router} \to \text{Verified expert} \to \text{Verified system}
\end{equation*}

Empirically, this compositional robustness manifests as consistent adversarial accuracy across heterogeneous datasets, even when experts differ in architecture or domain. When trained adversarially, the system exhibited a 15--17\% gain in overall adversarial accuracy with only a modest drop in clean accuracy, confirming that robustness can propagate modularly without full retraining. The results also highlight that the weakest expert constrains system-level robustness, this is an important insight for future adaptive routing strategies that dynamically weight experts based on per-sample uncertainty or certified margins.

\subsection{Scalability and Lifelong Learning Implications}

Beyond robustness, compositional design enables substantial efficiency gains. Freezing pre-trained experts and retraining only the router reduced incremental training time by 16.7\% for multi-dataset integration, demonstrating scalability for lifelong learning. Inference latency scaled linearly with the number of activated experts $O(k)$, validating the expected sublinear behavior relative to total model size. These results confirm that heterogeneous MoE systems can be both verifiable and computationally scalable, addressing two traditionally conflicting objectives.

This modular structure also provides a practical pathway for continual deployment: new experts can be integrated without re-verifying the entire model, provided that the router's certified decision region remains invariant. This property aligns with formal compositional verification theory, which emphasizes modular certification as a scalable alternative to full-system proofs.

\subsection{Limitations and Future Directions}

The current experiments are limited to small CNN architectures designed for verification compatibility. Scaling formal analysis to deeper or transformer-based experts remains computationally expensive due to verifier memory constraints and nonlinearity growth. Another limitation is that the router's perfect gating accuracy may rely on dataset dissimilarity; overlapping domains (e.g., regional traffic sign variants) could reduce compositional stability. Future work will address these issues through:

\begin{itemize}
    \item Probabilistic verification (e.g., conformal or randomized smoothing) to approximate CRA for larger models.
    \item Adaptive gating mechanisms that use reinforcement learning or uncertainty-aware routing to handle similar datasets.
    \item Hybrid training regimes that combine adversarial and self-supervised objectives to improve clean accuracy without sacrificing robustness.
\end{itemize}

Ultimately, the findings suggest that empirical robustness and formal verification are not opposing paradigms but components of a unified robustness framework. By designing architectures that are verification-aware yet empirically effective, heterogeneous MoE systems like MetaMoE can achieve both provable safety guarantees and scalable, real-world performance.

\section{Conclusion}

This paper presented a framework for analyzing and verifying the compositional robustness of heterogeneous Mixture-of-Experts systems through both empirical and formal perspectives. By combining Adversarially-trained experts with a verifiable gating network, the proposed MetaMoE architecture demonstrates that robustness can propagate modularly and be formally certified using $\alpha$--$\beta$-CROWN.

Empirical experiments confirmed that adversarial robustness accuracy strongly correlates with certified robustness accuracy, validating the hypothesis that empirical robustness serves as a practical predictor of verifiable safety.

Furthermore, the modular training strategy achieved measurable scalability gains, reducing retraining costs and maintaining linear inference complexity, while preserving robustness guarantees. These results establish that robust and scalable MoE architecture can be both empirically resilient and formally provable, providing a pathway toward certifiably safe, compositional AI systems suitable for safety-critical application.

These findings underscore that empirical and formal robustness are complementary facets of a unified robustness paradigm. Through careful architectural design and modular verification, heterogeneous MoE systems can achieve both high performance and certifiable safety and advancing the path toward trustworthy, compositional AI for real-world deployment.

\section{References}

\begin{thebibliography}{99}

\bibitem{jacobs1991}
Jacobs, R. A., Jordan, M. I., Nowlan, S. J., \& Hinton, G. E. (1991). Adaptive mixtures of local experts. \textit{Neural Computation}, 3(1), 79-87.

\bibitem{shazeer2017}
Shazeer, N., Mirhoseini, A., Maziarz, K., Davis, A., Le, Q., Hinton, G., \& Dean, J. (2017). Outrageously large neural networks: The sparsely-gated mixture-of-experts layer. arXiv preprint arXiv:1701.06538.

\bibitem{riquelme2021}
Riquelme, C., Puigcerver, J., Mustafa, B., Neumann, M., Jenatton, R., Susillo, D., \& Vinyals, O. (2021). Scaling vision with sparse mixture of experts. \textit{Advances in Neural Information Processing Systems}, 34, 8583-8595.

\bibitem{tu2023}
Tu et al. (2023). MOAT: Alternating Mobile Convolution and Attention Brings Strong Vision Models. arXiv:2210.01820.

\bibitem{fedus2023}
Fedus, W., Zoph, B., \& Shazeer, N. (2022). Switch transformers: Scaling to trillion parameter models with simple and efficient sparsity. \textit{Journal of Machine Learning Research}, 23(120), 1-39.

\bibitem{dosovitskiy2021}
Dosovitskiy, A., Beyer, L., Kolesnikov, A., Weissenborn, D., Zhai, X., Unterthiner, T., ... \& Houlsby, N. (2021). An image is worth 16x16 words: Transformers for image recognition at scale. In \textit{International Conference on Learning Representations}.

\bibitem{liu2022}
Liu, Z., Mao, H., Wu, C. Y., Feichtenhofer, C., Darrell, T., \& Xie, S. (2022). A ConvNet for the 2020s. In \textit{Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition} (pp. 11976-11986).

\bibitem{pfeiffer2023}
Pfeiffer, J., Kamath, A., Goyal, N., Rebuffi, S. A., Pratap, A., \& Ruder, S. (2023). Modular deep learning. arXiv preprint arXiv:2302.11529.

\bibitem{jiang2024}
Jiang, A. Q., et al. (2024). Mixtral of experts. arXiv preprint arXiv:2401.04088.

\bibitem{clarke2024}
Clarke, R. (2024). Build a hard mixture of pre-trained experts in PyTorch. \textit{Medium}. Available at: \url{https://medium.com/@rjnclarke/build-a-hard-mixture-of-pre-trainedexperts-in-pytorch6b602bae0f2c}

\bibitem{guo2018}
Guo, J., Shah, D. J., \& Barzilay, R. (2018). Multi-source domain adaptation with mixture of experts. arXiv preprint arXiv:1809.02256.

\bibitem{wang2024}
Wang, A., et al. (2024). HMoE: Heterogeneous mixture of experts for language modeling. arXiv preprint arXiv:2408.10681.

\bibitem{eigen2017}
Eigen, D., Ranzato, M., \& Sutskever, I. (2014). Learning factored representations in a deep mixture of experts. In \textit{International Conference on Learning Representations}.

\bibitem{gtsrb}
Stallkamp, J., Schlipsing, M., Salmen, J., \& Igel, C. (2012). Man vs. computer: Benchmarking machine learning algorithms for traffic sign recognition. \textit{Neural Networks}, 32, 323-332.

\bibitem{ptsd}
Mogelmose, A., Trivedi, M. M., \& Moeslund, T. B. (2012). Vision-based traffic sign detection and analysis for intelligent driver assistance systems: Perspectives and survey. \textit{IEEE Transactions on Intelligent Transportation Systems}, 13(4), 1484-1497.

\bibitem{aljundi2017}
Aljundi, R., Chakravarty, P., \& Tuytelaars, T. (2017). Expert gate: Life-long learning with a network of experts. In \textit{Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition} (pp. 3366-3375).

\bibitem{ramasesh2023}
Ramasesh, V. V., Lewkowycz, A., \& Dyer, E. (2022). Effect of scale on catastrophic forgetting in neural networks. In \textit{International Conference on Learning Representations}.

\bibitem{wang2021}
Wang, S., Zhang, H., Xu, K., Lin, X., Jana, S., Hsieh, C. J., \& Kolter, J. Z. (2021). Beta-CROWN: Efficient bound propagation with per-neuron split constraints for neural network robustness verification. \textit{Advances in Neural Information Processing Systems}, 34, 29909-29921.

\bibitem{jordan1991}
Jordan, M. I., \& Jacobs, R. A. (1994). Hierarchical mixtures of experts and the EM algorithm. \textit{Neural Computation}, 6(2), 181-214.

\bibitem{yang2024}
Yang, Y., et al. (2024). Theory on mixture-of-experts in continual learning. arXiv preprint arXiv:2406.16437.

\bibitem{pham2023}
Pham, H., Kim, Y. J., Mukherjee, S., Woodruff, D., Poczos, B., \& Awadalla, H. (2023). Task-Based MoE for Multitask Multilingual Machine Translation. arXiv preprint arXiv:2308.15772.

\end{thebibliography}

\appendix

\section{Training Hyperparameters}

\subsection{Expert Training Configuration}

All experts trained with the following configuration:

\begin{table}[H]
\centering
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Optimizer & Adam \\
Learning Rate & $1 \times 10^{-3}$ \\
Weight Decay & 0 \\
Loss Function & Label Smoothing Cross Entropy (smoothing = 0.1) \\
Epochs & 200 (early stopping on validation plateau) \\
Batch Size & 128 \\
Data Augmentation & Random horizontal flips, random crops ($28 \times 28 \to 32 \times 32$) \\
\midrule
\multicolumn{2}{l}{\textbf{Adversarial Training (RT only)}} \\
\midrule
Attack Type & PGD (Projected Gradient Descent) \\
Perturbation Budget ($\varepsilon$) & $8/255 = 0.03137$ \\
PGD Iterations & 7 \\
PGD Step Size ($\alpha$) & $2/255 = 0.00784$ \\
Norm & $L_\infty$ \\
\bottomrule
\end{tabular}
\caption{Expert Training Hyperparameters}
\end{table}

\subsection{Router Training Configuration}

\begin{table}[H]
\centering
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Optimizer & Adam \\
Learning Rate & $1 \times 10^{-3}$ \\
Weight Decay & $5 \times 10^{-4}$ \\
Loss Function & Cross Entropy \\
Epochs & 50 (early stopping on validation plateau) \\
Batch Size & 128 (mixed datasets) \\
Data Augmentation & Random horizontal flips, random crops \\
\midrule
\multicolumn{2}{l}{\textbf{Adversarial Training (RT only)}} \\
\midrule
Attack Type & PGD \\
Perturbation Budget ($\varepsilon$) & $8/255 = 0.03137$ \\
PGD Iterations & 7 \\
PGD Step Size ($\alpha$) & $2/255 = 0.00784$ \\
Norm & $L_\infty$ \\
\bottomrule
\end{tabular}
\caption{Router Training Hyperparameters}
\end{table}

\section{ONNX Export Details}

\subsection{BatchNorm Folding}

BatchNorm parameters are folded into convolutional layer weights during ONNX export using the following transformations:

For a convolutional layer with weights $W \in \mathbb{R}^{C_{out} \times C_{in} \times k \times k}$ and bias $b \in \mathbb{R}^{C_{out}}$, followed by BatchNorm with parameters:
\begin{itemize}
    \item Running mean: $\mu \in \mathbb{R}^{C_{out}}$
    \item Running variance: $\sigma^2 \in \mathbb{R}^{C_{out}}$
    \item Learnable scale: $\gamma \in \mathbb{R}^{C_{out}}$
    \item Learnable shift: $\beta \in \mathbb{R}^{C_{out}}$
    \item Epsilon: $\varepsilon = 10^{-5}$
\end{itemize}

The folded parameters are:

\begin{align}
W'_{i,j,m,n} &= \gamma_i \cdot \frac{W_{i,j,m,n}}{\sqrt{\sigma^2_i + \varepsilon}} \quad \forall i,j,m,n \\
b'_i &= \frac{\gamma_i}{\sqrt{\sigma^2_i + \varepsilon}} \cdot (b_i - \mu_i) + \beta_i \quad \forall i
\end{align}

This transformation is mathematically equivalent to the original Conv+BatchNorm sequence but eliminates the BatchNorm operation from the computational graph.

\textbf{Verification:} Folding correctness validated by comparing outputs: $\max|f_{\text{original}}(x) - f_{\text{folded}}(x)| < 10^{-5}$ for random inputs.

\subsection{ONNX Export Configuration}

All models exported with:
\begin{itemize}
    \item PyTorch version: 2.0+
    \item ONNX opset version: 13
    \item Model mode: \texttt{eval()} (deterministic behavior)
    \item Input shape: Static $(1, 3, 32, 32)$
    \item Output: Raw logits (no softmax)
    \item Simplification: Dead code elimination, constant folding
\end{itemize}

\textbf{Inspection checklist:}
\begin{enumerate}
    \item Zero BatchNorm operators
    \item Operator count $\leq 20$ for router, $\leq 20$ for experts
    \item Input/output dimensions match specification
    \item No dynamic shapes (Shape/Gather/Reshape eliminated)
\end{enumerate}

\section{Dataset Characteristics}

\subsection{Dataset Details}

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
\textbf{Dataset} & \textbf{Classes} & \textbf{Train Samples} & \textbf{Test Samples} & \textbf{Domain} \\
\midrule
CIFAR-10 & 10 & 50,000 & 10,000 & General objects \\
MNIST & 10 & 60,000 & 10,000 & Handwritten digits \\
GTSRB & 43 & 39,209 & 12,630 & German traffic signs \\
PTSD & 12 & 6,421 & 1,604 & Polish traffic signs \\
BTSD & 8 & 3,200 & 800 & Belgian traffic signs \\
ETSD & 15 & 7,500 & 1,875 & European traffic signs \\
TSRB & 20 & 10,000 & 2,500 & Traffic sign recognition benchmark \\
\bottomrule
\end{tabular}
\caption{Dataset Statistics}
\end{table}

\subsection{Dataset Selection Rationale}

\textbf{Domain diversity (CIFAR-10, MNIST, GTSRB):}
\begin{itemize}
    \item CIFAR-10: Natural images (animals, vehicles, objects) with high intra-class variance
    \item MNIST: Grayscale handwritten digits with low complexity and high separability
    \item GTSRB: Outdoor traffic signs with specific geometric constraints
\end{itemize}

These three datasets are visually and semantically distinct, enabling the router to learn robust decision boundaries based on fundamental domain characteristics. This diversity isolates the variable of dataset similarity, ensuring routing is based on learned domain features rather than confounding factors.

\textbf{Domain similarity (PTSD, BTSD, ETSD, TSRB):}

All four datasets contain traffic signs from different countries/regions. They share:
\begin{itemize}
    \item Overlapping shape vocabularies (circles, triangles, octagons)
    \item Similar color schemes (red, blue, yellow backgrounds)
    \item Common semantic categories (warnings, prohibitions, mandatory)
    \item Regional variations in design conventions
\end{itemize}

This controlled similarity tests the MoE's ability to handle overlapping domains, critical for lifelong learning scenarios where new experts are added incrementally (e.g., adapting to new geographic regions without retraining the entire system).

\section{Verification Implementation Details}

\subsection{Router Extraction Code}

Complete implementation for extracting router from MetaMoE:

\begin{lstlisting}[language=Python, caption={Router-only ONNX export}]
import torch
import torch.nn as nn

class SimplifiedRouter(nn.Module):
    """Wrapper to extract router for standalone verification"""
    def __init__(self, meta_gating_net):
        super().__init__()
        # Extract backbone (CNN feature extractor)
        self.backbone = meta_gating_net.model  # [batch, 3, 32, 32] -> [batch, 896]

        # Extract classification head (expert logits)
        self.fc = meta_gating_net.fc  # [batch, 896] -> [batch, N_experts]

    def forward(self, x):
        # x: [batch, 3, 32, 32] normalized input
        features = self.backbone(x)  # [batch, 896]
        logits = self.fc(features)   # [batch, N_experts]
        return logits  # Raw logits, NO softmax

# Export to ONNX
def export_router_to_onnx(meta_moe_model, output_path):
    # Extract router
    router = SimplifiedRouter(meta_moe_model.meta_gating_net)
    router.eval()

    # Dummy input (normalized space)
    dummy_input = torch.randn(1, 3, 32, 32)

    # Export with static shapes
    torch.onnx.export(
        router,
        dummy_input,
        output_path,
        input_names=['input'],
        output_names=['output'],
        dynamic_axes=None,  # Static shapes only
        opset_version=13
    )

    print(f"Router exported to {output_path}")
    return output_path
\end{lstlisting}

\subsection{VNNLIB Property Format}

Example VNNLIB specification for verifying MNIST sample (meta-class = 1):

\begin{verbatim}
; Input constraints: epsilon-ball around normalized image
(declare-const X_0 Real)  ; Pixel [0,0,0]
(declare-const X_1 Real)  ; Pixel [0,0,1]
...
(declare-const X_3071 Real)  ; Pixel [2,31,31]

; Epsilon = 2/255 = 0.00784
(assert (<= X_0 0.523))  ; x_norm[0] + epsilon
(assert (>= X_0 0.507))  ; x_norm[0] - epsilon
...

; Output variables
(declare-const Y_0 Real)  ; CIFAR-10 logit
(declare-const Y_1 Real)  ; MNIST logit

; Property: Verify Y_1 > Y_0 (MNIST has highest logit)
; NEGATION for counterexample search:
(assert (>= Y_0 Y_1))  ; Try to find Y_0 >= Y_1

; If UNSAT (no counterexample) => VERIFIED
; If SAT (counterexample found) => FALSIFIED
\end{verbatim}

\subsection{Unified Normalization Parameters}

Router normalization computed from training set statistics:

\begin{table}[H]
\centering
\begin{tabular}{llll}
\toprule
\textbf{Channel} & \textbf{Mean ($\mu$)} & \textbf{Std ($\sigma$)} & \textbf{Source} \\
\midrule
Red (0) & 0.295 & 0.325 & Average across all datasets \\
Green (1) & 0.291 & 0.321 & Average across all datasets \\
Blue (2) & 0.274 & 0.319 & Average across all datasets \\
\bottomrule
\end{tabular}
\caption{Unified Normalization Parameters}
\end{table}

\textbf{Consistency verification:}
\begin{itemize}
    \item \texttt{train\_moe.py:484-485}: UNIFIED\_NORM used in router training dataloaders
    \item \texttt{export\_router\_to\_abcrown.py}: Model weights implicitly contain normalization
    \item \texttt{prepare\_router\_verification.py:134,140}: VNNLIB bounds use UNIFIED\_NORM
    \item \texttt{verify\_all\_router\_samples.py}: Test images normalized with UNIFIED\_NORM
\end{itemize}

All components use the same normalization $\Rightarrow$ no distribution shift.

\section{Hardware and Software Environment}

\subsection{Hardware Specifications}

\textbf{Training and Inference Experiments:}
\begin{itemize}
    \item CPU: Intel Core i7-14700K (20 cores, 28 threads)
    \item GPU: NVIDIA GeForce RTX 4060 (8GB GDDR6)
    \item RAM: 64GB DDR5-5600
    \item Storage: 2TB NVMe SSD
    \item OS: Ubuntu 22.04.5 LTS
\end{itemize}

\textbf{Verification Experiments:}
\begin{itemize}
    \item Same hardware as training
    \item Gurobi optimizer: Version 11.0.0 (academic license)
    \item Timeout per sample: 300 seconds
    \item GPU memory limit: 8GB (OOM triggers "unknown" status)
\end{itemize}

\subsection{Software Environment}

\begin{table}[H]
\centering
\begin{tabular}{ll}
\toprule
\textbf{Package} & \textbf{Version} \\
\midrule
Python & 3.10.12 \\
PyTorch & 2.1.0+cu121 \\
torchvision & 0.16.0+cu121 \\
CUDA & 12.1 \\
ONNX & 1.15.0 \\
alpha-beta-CROWN & commit d4c79e3 (Dec 2024) \\
Gurobi & 11.0.0 \\
NumPy & 1.24.3 \\
Matplotlib & 3.7.2 \\
tqdm & 4.66.1 \\
Adversarial Robustness Toolbox (ART) & 1.17.0 \\
\bottomrule
\end{tabular}
\caption{Software Dependencies}
\end{table}

\subsection{Reproducibility Notes}

All experiments use fixed random seeds:
\begin{itemize}
    \item PyTorch: \texttt{torch.manual\_seed(42)}
    \item NumPy: \texttt{np.random.seed(42)}
    \item Python: \texttt{random.seed(42)}
    \item CUDA: \texttt{torch.backends.cudnn.deterministic = True}
\end{itemize}

Training time and inference latency measurements exclude data loading and model initialization (only forward/backward passes and optimizer steps).

\end{document}

